/*Escreva um programa que leia5 números inteiros em qualquer
ordem e exiba na tela ao final, os cinco números em ordem crescente.
*/

#include <stdio.h>
#include <locale.h>

int main (){
	setlocale (LC_ALL, "");
	int a, b, c, d, e;
	
	printf ("Digite cinco números inteiros separados por espaço: ");
	scanf ("%d", &a);
	scanf ("%d", &b);
	scanf ("%d", &c);
	scanf ("%d", &d);
	scanf ("%d", &e);
	
	if ()
}
